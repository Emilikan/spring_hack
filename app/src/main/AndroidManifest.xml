<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.springhack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MediumEnemy"/>
        <activity android:name=".Review" />
        <activity android:name=".EnemyLeft" />

        <activity android:name=".SignUp"/>
        <activity android:name=".SignIn"/>

        <activity android:name=".AddTask"></activity>
        <activity android:name=".Tasks" />
        <activity android:name=".ProfileTeamlid" />
        <activity android:name=".Hero" />
        <activity android:name=".BigBoss" />
        <activity android:name=".Enemy" />
        <activity android:name=".SmallBoss" />
        <activity android:name=".Participants" />
        <activity android:name=".BeforeEnemy"/>
        <activity android:name=".Artefact" />
        <activity android:name=".Profile"/>

        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>